pkgname="sleex-user-config"
pkgver="1.1"
pkgrel="1"
pkgdesc="User configuration for Sleex desktop environment"
arch=("x86_64")
depend=("sleex")


package(){
   mkdir -p ${pkgdir}/etc/skel/
   cp -r ${srcdir}/.config/ ${pkgdir}/etc/skel/
}
